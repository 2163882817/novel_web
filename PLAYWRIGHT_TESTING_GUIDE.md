# Playwright 新手入门与本项目测试需求分析

> 文档目的：帮助零基础测试人员了解如何用 Playwright 规划和执行端到端测试，并为本仓库的 AI 小说写作台建立功能测试与 UI 测试基线。本文只描述测试设计，不提供测试代码。

## 1. Playwright 是什么

Playwright 是用于 Web 自动化测试的工具，能够驱动 Chromium、Firefox 和 WebKit 三种浏览器。它适合验证用户从打开网页到完成业务操作的完整过程，也能检查接口响应、下载文件、弹窗、文件上传和流式响应等浏览器行为。

对本项目而言，Playwright 的重点价值是验证 Vue 前端与 FastAPI 后端的协作：用户创建小说、维护章节与设定、触发 AI 写作、保存结果、导出文本，以及在失败时获得清晰反馈。

## 2. 新手学习路径

### 2.1 先建立正确的测试观念

- 一个测试只验证一个清楚的用户目标，例如“新建小说后进入该小说工作台”。
- 断言业务结果，而不是断言实现细节。优先检查用户可见文字、可用状态、页面跳转、保存后的数据，而不是 Vue 内部变量或 CSS 类名。
- 测试必须独立。每个用例自行准备数据并清理，不能依赖前一个用例创建的小说或章节。
- AI 服务具有随机性、耗时和外部依赖；自动化回归应使用稳定的模拟响应，真实模型连通性另设少量冒烟测试。

### 2.2 安装与初始化（不含测试代码）

1. 安装 Node.js 18 或更高版本，建议使用 20 LTS。
2. 在 `frontend` 目录安装项目依赖。
3. 使用 Playwright 官方初始化命令 `npm init playwright@latest` 创建测试工程；选择 JavaScript、HTML 报告和浏览器下载。
4. 在配置中设置应用启动方式：前端为 Vite 服务，后端为 FastAPI 服务；基础访问地址通常是 `http://127.0.0.1:5173`。
5. 首次执行可使用 `npx playwright test --ui` 查看交互式运行界面；常规回归使用 `npx playwright test`；查看报告使用 `npx playwright show-report`。

不要把真实 API Key 写入测试配置、截图、录屏或报告。测试环境应使用独立数据库和虚拟 API Key。

### 2.3 认识一次测试的四个部分

| 部分 | 要回答的问题 | 本项目示例 |
| --- | --- | --- |
| 前置条件 | 测试开始前需要什么 | 后端已启动，存在一部测试小说 |
| 操作 | 用户如何完成目标 | 点击“写新章节”，填写细纲，开始生成 |
| 断言 | 什么结果代表成功 | 生成内容出现、保存按钮可用、章节出现在左侧列表 |
| 清理 | 如何避免影响下次运行 | 删除测试小说或重置测试数据库 |

### 2.4 定位元素的优先级

优先选择能表达用户意图、且不随排版变化的定位方式：

1. 可访问角色与名称，例如按钮的可见名称“新建小说”。
2. 表单标签与占位提示，例如“书名”“章节标题”。
3. 专门提供的 `data-testid` 属性（仅在文字可能变化、元素重复或名称不唯一时使用）。
4. 文本定位只适合稳定的可见文案。

避免以 DOM 层级、序号、随机生成的 class 名或坐标定位。例如“第 3 个按钮”和 `.row > div:nth-child(2)` 会在 UI 调整后迅速失效。

### 2.5 自动等待与断言

Playwrigh，而不是人为加入固定等待时间。对t 会等待元素可见、可交互和页面跳转完成；测试设计应利用断言等待最终状态本项目的 AI 流式生成，等待条件应是“编辑器已有内容”“生成中状态消失”或“出现完成统计”，不应固定等待若干秒。

页面或接口失败时，保留截图、视频、trace 和浏览器控制台日志。它们能区分前端交互问题、后端返回错误和第三方模型不可用。

### 2.6 测试分层建议

| 层级 | 目标 | 适用范围 |
| --- | --- | --- |
| 冒烟测试 | 确认核心路径可用 | 打开书架、创建小说、保存章节、导出 |
| 端到端回归 | 验证用户完整流程 | 小说—设定—写作—校对—定稿—导出 |
| 接口契约测试 | 验证输入、状态码、响应结构 | 小说、章节、导入、记忆、配置 API |
| 视觉回归测试 | 发现布局和样式变化 | 书架、工作台、设定页、配置页 |
| 真实服务冒烟 | 检查部署环境与模型可连通 | 健康检查、配置测试，不纳入稳定回归门禁 |

### 2.7 推荐的工程组织方式

- 用例按业务模块组织：书架、工作台、设定、配置、导出。
- 将重复步骤抽成页面对象：创建小说、打开章节、保存章节、删除测试数据。
- 将普通数据、边界数据、非法数据和 AI 模拟响应分开管理。
- 为本项目补充稳定的可访问名称或 `data-testid`，这是提高自动化稳定性的前置优化；不要依赖图标按钮本身。
- 在 CI 中先执行 Chromium 冒烟和 API 契约测试；夜间执行全浏览器回归与视觉快照。

## 3. 项目测试对象与范围

### 3.1 已识别的系统结构

项目是本地优先的 AI 小说写作台：前端使用 Vue 3、Vite 和 Vue Router；后端使用 FastAPI、SQLAlchemy 与 SQLite。前端通过 `/api` 调用后端，AI 写作采用 SSE 流式响应；模型配置中的 API Key 在后端加密保存。

当前主要页面和路由如下：

| 页面 | 路由 | 主要业务 |
| --- | --- | --- |
| 书架 | `/` | 小说列表、新建小说、删除小说 |
| 小说工作台 | `/novel/:id` | 章节列表、细纲、流式写作、A/B/C 变体、章节编辑、校对、修订、定稿、导出 |
| 设定与记忆 | `/novel/:id/settings` | 人物卡、伏笔、卷管理、文档导入、写作限制词 |
| API 配置 | `/config` | Base URL、API Key、模型名、温度、连通性测试 |

不在本轮范围内：登录与权限、多人协作、支付、跨设备同步。它们在当前代码中没有对应功能。

### 3.2 核心业务链路

创建小说 → 补充人物/大纲/限制设定 → 生成或编辑章节细纲 → AI 写作或选择变体 → 保存章节 → 校对并选择问题 → AI 修订 → 应用修订 → 定稿更新记忆 → 选择章节导出 TXT。

此链路应作为最高优先级端到端回归场景，AI 生成、校对和修订步骤使用可预测的测试替身，以保证结果可重复。

## 4. 功能测试需求分析

### 4.1 P0：发布前必须通过

| 编号 | 模块 | 场景与预期结果 |
| --- | --- | --- |
| F-01 | 服务可用性 | 前后端启动后，书架成功加载；健康检查返回正常状态。 |
| F-02 | 新建小说 | 书名为空时阻止提交并提示；填写书名后创建成功，自动进入对应工作台。 |
| F-03 | 小说隔离 | 不同小说的章节、人物、伏笔和导出内容互不混入。 |
| F-04 | 章节保存 | 手工或生成内容为空时不能保存；保存成功后标题、正文、字数和章节序号正确显示。 |
| F-05 | 章节编辑 | 修改标题、正文和细纲后保存，刷新页面仍显示新内容。 |
| F-06 | 章节删除 | 删除前需要确认；确认后章节从列表与详情消失，取消则不改变数据。 |
| F-07 | 流式写作 | 请求期间显示生成中并禁用重复提交；内容逐步写入；停止后不再追加内容；失败显示错误且页面可继续操作。 |
| F-08 | 导出 TXT | 未选章节时不能导出；选择章节后下载成功，文件名可用，正文顺序正确；“包含细纲”选项生效。 |
| F-09 | 配置保护 | 获取配置不返回完整 API Key，只返回是否已保存与末尾掩码；保存空 Key 时保留已有 Key。 |
| F-10 | 错误处理 | 后端 400、404、500 以及网络中断时，页面显示可理解错误，不出现无响应或无限加载。 |

### 4.2 P1：重要回归场景

| 编号 | 模块 | 场景与预期结果 |
| --- | --- | --- |
| F-11 | 书架 | 空书架、加载中、加载失败和多本小说卡片都正确呈现；删除小说级联删除其数据。 |
| F-12 | AI 细纲 | 生成细纲后写入编辑区并同步章节标题；服务失败时保留用户原输入。 |
| F-13 | A/B/C 变体 | 未填写细纲不可触发；成功后展示所有变体；导入主编辑器、复制、重新生成均符合预期。 |
| F-14 | 标题生成 | 新章节与已保存章节都可生成候选标题；选择候选项正确写入对应标题输入框。 |
| F-15 | 校对与修订 | 校对问题默认全选；取消所有问题后不能修订；修订预览可应用或放弃，应用后正文更新。 |
| F-16 | 定稿与记忆 | 定稿后章节状态更新，摘要、角色状态、伏笔、卷摘要等记忆数据按后端结果刷新。 |
| F-17 | 人物卡 | 新建、编辑、删除人物卡；名称、角色和卡片字段保存正确；非法小说 ID 返回明确错误。 |
| F-18 | 伏笔库 | 新建、编辑、删除伏笔；状态切换与描述保存正确。 |
| F-19 | 卷管理 | 新建卷的编号与初始状态正确；编辑卷大纲；生成卷大纲和卷摘要的加载与异常状态正确。 |
| F-20 | 文档导入 | 上传或粘贴 `.md`/`.txt`；故事圣经、总纲、人物卡、限制词的 replace/append 语义正确；空文本和格式不合法有提示。 |
| F-21 | 限制词 | 导入、追加、清空限制词正确；清空需要避免误操作（至少确认或明确反馈）。 |
| F-22 | 路由异常 | 不存在或已删除的小说 ID 进入工作台/设定页时有友好失败反馈，不能呈现误导性的空白编辑器。 |

### 4.3 边界与异常数据

- 书名、章节标题、人物名为空、纯空格、超长文本、特殊符号和换行。
- 目标字数为 0、负数、非数字、极大数；温度为边界 0 与 2，以及范围外值。
- 章节大量、正文超长、多卷、多人物、多伏笔时的加载和排序。
- 导入空文件、非 `.md/.txt` 文件、编码异常文件、格式不符合解析约定的文件。
- SSE 返回空流、非 JSON 事件、错误事件、没有 done 事件、用户中止请求。
- 下载被浏览器阻止、后端导出失败、文件名包含中文或特殊字符。
- 后端接口超时、配置未完成、模型鉴权失败、模型限流。

## 5. UI 测试需求分析

### 5.1 视觉与布局验收

至少建立四页基准截图：空书架、含小说的书架、小说工作台、设定页、API 配置页。每次视觉回归在固定浏览器、固定时区、固定字体和固定测试数据下比对，避免动态时间与 AI 生成文本造成噪声。

| UI-编号 | 页面/组件 | 检查项 |
| --- | --- | --- |
| U-01 | 全局布局 | 页头、内容区、主操作按钮层级清晰；页面滚动时内容不遮挡。 |
| U-02 | 书架 | 空状态、加载状态、错误状态、卡片网格、标签和删除按钮对齐；长书名不挤压或覆盖删除按钮。 |
| U-03 | 新建小说弹窗 | 遮罩覆盖正确；点击遮罩关闭；焦点进入首个输入框；提交/取消按钮状态明确。 |
| U-04 | 工作台 | 章节列表、写作区、编辑区、消息区层级合理；长正文可编辑、可滚动且不溢出。 |
| U-05 | AI 反馈 | 生成中、成功、失败、停止、禁用态均有明显而一致的视觉反馈；不允许双击产生重复请求。 |
| U-06 | 变体与校对 | 变体卡片可辨别、导入与复制操作不混淆；问题选择框、修订预览与应用/放弃操作清晰。 |
| U-07 | 导出弹窗 | 章节勾选、全选、清空、包含细纲和主按钮状态一致；长章节标题不截断关键信息。 |
| U-08 | 设定页 | Tab 激活态清晰；人物、伏笔、卷、导入区切换不残留上一个 Tab 的表单状态。 |
| U-09 | 配置页 | API Key 输入掩码；已保存 Key 只显示尾号；成功与错误信息颜色、文本和对比度明确。 |
| U-10 | 响应式 | 至少验证 1440×900、1280×800、768×1024、390×844；无横向溢出，按钮可点，弹窗可完整操作。 |

### 5.2 可用性与可访问性

- 所有交互控件都具有可见名称；图标按钮应提供准确的 `aria-label` 或文本提示。
- Tab 键可按合理顺序到达输入框、按钮、勾选框和弹窗内控件；弹窗打开后焦点不应落到背景页面。
- 按 Enter/Space 可激活可聚焦按钮；原生确认框、文件选择和下载流程应可操作。
- 输入错误应以文本说明，不能只依赖颜色；成功、警告、错误提示应满足足够的色彩对比度。
- 中文文案不得出现乱码、截断或占位符残留。重点检查 API 错误、下载文件名、导入结果和 AI 状态提示。

## 6. 自动化实施建议

### 6.1 数据与环境隔离

- 每次测试使用独立 SQLite 数据库或在套件前后清理测试数据，禁止连接开发者的真实数据文件。
- 使用固定测试小说、章节、人物、伏笔与导入文本，并在标题中加唯一测试前缀，便于排查残留。
- 拦截或替代所有会调用外部大模型的请求，返回固定的细纲、SSE 片段、校对报告、修订稿、卷摘要和标题候选。
- 配置一个单独的真实服务冒烟环境，只验证模型配置测试按钮，不把生成文本内容作为稳定断言。

### 6.2 建议的执行顺序

1. Chromium 冒烟：F-01 至 F-10，作为每次提交门禁。
2. API 契约与 P1 回归：在合并请求阶段执行。
3. Firefox、WebKit 与响应式 UI 用例：每日或每次发布前执行。
4. 视觉快照与真实模型冒烟：夜间或发布候选环境执行。

### 6.3 失败诊断规范

- 失败用例自动保留截图、视频和 trace。
- 对接口失败记录请求路径、状态码与脱敏后的响应内容；严禁记录 API Key。
- SSE 用例额外记录是否收到首个增量、错误事件和完成事件，便于定位“前端未消费流”还是“后端提前中断”。
- 每个缺陷应标注影响模块、复现数据、浏览器、截图/trace、期望结果和实际结果。

## 7. 当前风险与测试关注点

1. AI 输出不可预测且可能很慢，若直接依赖真实模型，端到端用例会脆弱且成本高；应优先模拟。
2. 流式写作涉及中止、分片、错误事件与完成事件，是最需要专项覆盖的异步交互。
3. 导入功能包含多种文档解析与 replace/append 语义，应使用典型、边界和异常格式分别验证。
4. 本地 SQLite 与配置加密文件属于敏感持久化数据；测试数据、导出文件、截图与报告都不能泄露真实小说内容或密钥。
5. 现有按钮多依赖可见文本和图标，建议在实施自动化前补充稳定的可访问名称或测试标识，尤其是删除、标题生成、复制、停止和清空等图标/高风险操作。

## 8. 首批用例清单

建议先实现以下 12 条自动化场景，以最快形成可靠反馈：

1. 打开书架并处理空列表。
2. 创建小说，验证跳转与书架卡片。
3. 书名为空时的前端校验。
4. 手工保存章节并刷新校验持久化。
5. 编辑并删除章节，分别验证确认和取消。
6. 模拟 SSE 写作，验证增量内容、完成状态与停止。
7. 模拟 A/B/C 变体并导入主编辑器。
8. 校对、选择问题、修订预览和应用修订。
9. 选择章节导出 TXT，并检查下载内容。
10. 创建与维护人物卡、伏笔。
11. 导入人物卡与限制词，分别验证 replace/append。
12. API 配置保存、Key 脱敏和连接失败提示。

这些场景完成后，再扩展卷管理、跨浏览器、响应式和视觉回归。这样能以最小成本覆盖数据丢失、重复写作、错误导出、密钥泄露和 AI 异步失败等高风险问题。
